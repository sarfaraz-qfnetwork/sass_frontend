import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: {
      100: "#BBFFDD",
      500: "#48BB78", // Primary Color
      600: "#38A169",
    },
  },
});

export default theme;
